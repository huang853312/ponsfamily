**HyperEVM 实际执行检查：2026-09-14，北京时间 19:50**

结论：扫描停滞已修复并恢复补扫；身份确认和正式项目通知仍未打通。不能把 systemd active 或单项部署成功称为整条雷达成功。

| 严重级别 / 状态 | 堵点与真实原因 | 已执行动作 / 当前边界 |
|---|---|---|
| P0，已修复 | 合约 0xba8b2199f72369d95660276a88fbe1eae8c5a278 在区块 45872378 的 ERC-20 查询返回 code -32003、EVM error: InvalidFEOpcode。此前元数据修复遗漏这种明确的执行失败，把它当成临时 RPC 故障；同一事件曾重试 170 次。 | 严格区分该错误与限流/未知 RPC 错误。事件现为 completed，175 次处理尝试，last_error 为空，按原规则入库 B 级。未手动跳过区块。 |
| P1，补扫进行中 | 09:05:37 UTC 起停在旧游标，积压约 9,470 块。补扫中公共 RPC 仍返回 -32005 rate limited。 | 保留游标和逐块重试。19:50:19 链头 45882423、next_block 45873220，积压 9,204 块；19:50:39 游标继续到 45873313。当前进程 37 次限流重试，0 次 InvalidFEOpcode 异常。尚未追平。 |
| P1，待用户决定调查流程 | 同一轮先做大量地址/成员/GitHub/文档地址查询。真实 Family 6（Pear Vault）用满 45.14 秒后，14 条后续查询未执行，名称辅助搜索和网页核验被挤掉。重试会再次从前面的查询开始。 | 已增加共享时间预算、执行失败标识，以及失败提供方 60 秒冷却；未调整查询顺序、各阶段配额。建议给地址发现、名称辅助、网页核验分别保留时间，并记录未完成查询进度。 |
| P1，待用户决定候选读取规则 | 若搜索只给出首页，首页没有 CA/名称线索，而 CA 位于首页直接链接的文档，当前网站筛选可能在读取文档前就丢弃首页。现有正例测试“homepage_without_address_docs_address_verifies”和“unrelated_deployer_token_confirmed_from_official_docs”仍失败。 | 建议候选先读取其直接关联文档/X，再按原有地址证据或官网/X 交叉核验确认；不能通过放行所有搜索结果来修。尚未证明本次 Pear 被剔除的搜索结果里含有真实官网。 |
| P1，执行层已修；正式送达未验收 | 合约先入库、Telegram 发送失败后，重扫被 seen 拦住；身份确认和 PT_MATCH 也存在“先存状态、后发送”的窗口。 | 增加 contract_processing、telegram_outbox、重启后的发送租约恢复；PT_MATCH 记录与通知同事务提交。仅 Telegram JSON ok=true 才确认发送成功。故障测试通过；生产 outbox 仍为空，没有新正式项目通知可证明端到端送达。 |
| P1，常规全量发布仍有测试阻塞 | 部署前旧生产源运行 58 项测试，6 项失败；新源运行 71 项，65 通过、同样 6 项失败、0 error。 | 13 项新增执行恢复测试通过；本次限定补丁要求生产快照哈希一致且不得增加失败，未宣称全量测试通过。6 项包括真实候选遍历问题，也包括旧的 PARTIAL/候选字段断言，不等于 6 个独立生产故障。 |
| P2，业务配置边界 | KNOWN_RWA_ASSETS 为空，RWA 配对提醒分支不会触发。 | 未填入任何未核验股票资产。需要决定监控哪些已有官方链上地址证据的 RWA 资产。 |
| P2，扫描范围与验证边界 | 当前读取顶层部署、既有工厂创建事件、V2/V3 建池签名。没有 Factory 地址白名单。任意内部 CREATE/CREATE2、非标准池不在完整覆盖保证内。 | 标准池核验失败也持久化原始候选。当前 1 条 VERIFIED、2 条 STANDARD_VALIDATION_FAILED；后者不能直接当成假池。扩展内部创建或非标准池适配需要另行决定。 |

已确认：
- 生产虚拟环境保留；19:45:14 启动的 PID 1623195 在部署后和后续审计中均 active/running，NRestarts=0。
- 部署门禁确实拦截了“服务 active、游标不动”的第一次冷却补丁，并自动回滚。查明 VM 错误后才再次部署。
- 四个搜索 API 的 Key 均来自运行进程。本次 LangSearch HTTP 200 / code 200 / 原始结果 5 条；You、Exa 也有 5 条，TinyFish 部分查询 9 条。成功调用不代表结果与目标相关；经现有地址相关性过滤后没有候选。
- Hyperevmscan 返回 403；DuckDuckGo 两个入口返回 403 后进入冷却，没有在后续每条查询反复访问。
- 当前 58 个身份调查均 NO_DATA，执行状态为 BUDGET_EXHAUSTED，下一次具备重试资格的退避为 300 秒。实际再次执行还受批次和 600 秒调度间隔影响；不能说保证 5 分钟内完成。
- 原有地址确认、官网/X 交叉确认、A/B/S 分级、PT_MATCH 条件未放宽。修复了 X 回链发现官网后漏重新计算官网到 X 链接的执行顺序错误。

接下来：
1. 服务继续从保存游标补扫；验收要看到积压归零，并核对失败事件完成状态。修复前两天的所有历史缺口尚未全面补齐验收。
2. 用户决定是否允许上述“分阶段时间分配 / 查询进度”和“候选先读取直接关联证据”两项流程调整；最终身份确认门槛保留。
3. 调查流程通过正反例回归后，再做真实项目发现、身份确认、Telegram 实际送达的正式全流程验收。当前没有这条成功证据。

证据：
- [第一次执行修复和生产验收](https://github.com/huang853312/ponsfamily/actions/runs/34802265625)
- [冷却补丁因扫描不动而回滚](https://github.com/huang853312/ponsfamily/actions/runs/34839133417)
- [真实卡块及四个方法 InvalidFEOpcode 响应](https://github.com/huang853312/ponsfamily/actions/runs/34839422722)
- [VM 错误修复部署成功及 71 项测试结果](https://github.com/huang853312/ponsfamily/actions/runs/34839764092)
- [真实身份调查与已恢复事件；末尾审计 RPC 被限流](https://github.com/huang853312/ponsfamily/actions/runs/34839990459)
- [带审计重试的持续补扫检查成功](https://github.com/huang853312/ponsfamily/actions/runs/34840215957)
- [VM 错误分类修复代码](https://github.com/huang853312/ponsfamily/commit/f9ed06e1a46bde9786b581a2abe30ea6dca07e61)
