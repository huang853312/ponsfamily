name: Deploy HyperEVM Radar

on:
  push:
    branches:
      - main
    paths:
      - "hyperevm-radar/**"
      - ".github/workflows/deploy-hyperevm-radar.yml"
  workflow_dispatch:

concurrency:
  group: deploy-hyperevm-radar-production
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  test-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install test dependencies
        run: python3 -m pip install --disable-pip-version-check aiohttp requests web3

      - name: Test HyperEVM Radar
        working-directory: hyperevm-radar
        run: |
          PYTHONPATH=. python3 -m unittest discover -s tests -v
          python3 -m compileall -q .

      - name: Configure SSH
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_PORT: ${{ secrets.SERVER_PORT }}
          SERVER_SSH_KEY: ${{ secrets.SERVER_SSH_KEY }}
        run: |
          install -d -m 700 ~/.ssh
          printf '%s\n' "$SERVER_SSH_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -p "$SERVER_PORT" -H "$SERVER_HOST" >> ~/.ssh/known_hosts

      - name: Back up and deploy HyperEVM Radar
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
          SERVER_PORT: ${{ secrets.SERVER_PORT }}
        run: |
          SSH="ssh -i $HOME/.ssh/deploy_key -p $SERVER_PORT"

          $SSH "$SERVER_USER@$SERVER_HOST" \
            'sudo test -d /opt/hyperevm-radar && sudo cp -a /opt/hyperevm-radar "/opt/hyperevm-radar.backup.$(date -u +%Y%m%dT%H%M%SZ)"'

          rsync -az --delete \
            --exclude='.env' \
            --exclude='data/' \
            --exclude='radar.db*' \
            --exclude='addresses.json' \
            --exclude='__pycache__/' \
            --exclude='*.pyc' \
            -e "$SSH" \
            --rsync-path='sudo rsync' \
            hyperevm-radar/ \
            "$SERVER_USER@$SERVER_HOST:/opt/hyperevm-radar/"

      - name: Restart and verify HyperEVM Radar
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
          SERVER_PORT: ${{ secrets.SERVER_PORT }}
        run: |
          ssh -i ~/.ssh/deploy_key -p "$SERVER_PORT" "$SERVER_USER@$SERVER_HOST" '
            set -e
            sudo systemctl restart hyperevm-radar.service
            sudo systemctl is-active hyperevm-radar.service
            sudo systemctl status hyperevm-radar.service --no-pager
            sudo journalctl -u hyperevm-radar.service -n 20 --no-pager
          '
